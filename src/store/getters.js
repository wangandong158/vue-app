const getters = {
  userName: state => state.user.userName,
  userAccount: state => state.user.userAccount,
  userCode: state => state.user.userCode,
  catchPages: state => state.app.catchPages,
}
export default getters
